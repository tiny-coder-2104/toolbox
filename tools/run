#!/bin/bash
# Run any tool with node16 (node20 broken on glibc: GLIBC_2.28 not found)
# Usage: tools/run <tool.js> [args...]  (from anywhere)
NODE16=/home/yuki/.nvm/versions/node/v16.20.2/bin/node
TOOL="$1"; shift
exec "$NODE16" "$(dirname "$0")/$TOOL" "$@"